{% extends layout %}

{% block contenido %}
    <div class="col_drcha">
	{% include '/_global/ruta.html.twig' %}
	<h1>Eventos</h1>

	<div id="col_izqda">
            {% for evento in values.eventos.left %}    
                {{ macro.noticia (evento) }} 
            {% endfor %}
	</div>


	<div id="col_drcha">
            {% for evento in values.eventos.right %}    
                {{ macro.noticia (evento) }} 
            {% endfor %}
	</div>
	
	{% include'Eventos/botonera.html.twig' %}
    </div>	
{% endblock %}