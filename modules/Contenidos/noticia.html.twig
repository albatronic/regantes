{# 
## Aquí tenemos que decirle, si solo hay 1 contenido desarrollado el h1 se queda tal cual, si hay más hay un titulo (de sección) por encima de h1
## Así que tenemos que forzar el h1 a que se convierta en h2. El h2 en h3 y el h3 en h4.
## Comprobar que he colocado bien la pregunta de que si el h1, h2, etc no está vacío, hay que pintarlo. Lo he comprobado con la fecha y no lo pinta.
## Contemplar la posibilidad de intercalar imágenes y texto. Párrafo + imagen + párrafo
#}


<div class="contenedorNoticia">
{% for contenido in values.contenidoDesarrollado %}    
    {% if contenido.fecha!="" %}<div class="fecha">{{contenido.fecha}}</div>{% endif %}

    {% if contenido.titulo!="" %}<h1>{{contenido.titulo}}</h1>{% endif %}

    {% if contenido.subtitulo!="" %}<h2>{{contenido.subtitulo}}</h2>{% endif %}

    {% if contenido.intro!="" %}<h3>{{contenido.intro}}</h3>{% endif %}

    {% if contenido.parrafo!="" %}<p>{{contenido.parrafo}}</p>{% endif %}

    {% if contenido.imagen.urlImagen!="" %}<div class="imagen_noticia"><img src="{{app.path}}/images/{{contenido.imagen.urlImagen}}" alt="{{contenido.imagen.nombreImagen}}" title="{{contenido.imagen.nombreImagen}}" /></div>{% endif %}

    {% if contenido.parrafo!="" %}<p>{{contenido.parrafo}}</p>{% endif %}

    {% if contenido.videoYoutube.video!="" %}
    <div class="videos_youtube">
            <iframe width="{{contenido.videoYoutube.ancho}}" height="{{contenido.videoYoutube.alto}}" src="http://www.youtube.com/embed/{{contenido.videoYoutube.video}}" frameborder="{{contenido.videoYoutube.anchoBorde}}"></iframe>
    </div>
    {% endif %}
    
{% endfor %}    
</div>